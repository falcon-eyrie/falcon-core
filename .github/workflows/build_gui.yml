name: Build GUI

on:
  pull_request:
    branches: [develop]
    paths:
      - "falcon_gui/**"
      - ".github/workflows/build_gui.yml"

jobs:
  build-linux:
    name: Build GUI
    runs-on: ubuntu-24.04

    defaults:
      run:
        working-directory: falcon_gui

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run CSpell spell checker
        uses: streetsidesoftware/cspell-action@v8
        with:
          # TODO(ben): enable strict checking once the codebase is cleaned
          strict: false

      - name: Install FVM and Flutter
        working-directory: . # To install FVM globally for the entire workflow
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev
          curl -fsSL https://fvm.app/install.sh | bash
          echo "/home/runner/fvm/bin" >> $GITHUB_PATH
          echo "/home/runner/fvm/versions/3.44.0/bin" >> $GITHUB_PATH
          export PATH="/home/runner/fvm/bin:$PATH"

          fvm install 3.44.0
          fvm global 3.44.0

      - name: Lint and format
        run: |
          flutter pub get
          dart format --set-exit-if-changed .
          flutter analyze --fatal-infos --fatal-warnings

      - name: Build project
        run: |
          flutter build linux --release

      - name: Publish artifact
        uses: actions/upload-artifact@v4
        with:
          name: falcon_gui
          path: falcon_gui/build/linux/x64/release/bundle/
          if-no-files-found: error
