/*



source:
    class: NlxPureReader
    options:
        nchannels: 32
        format: compact


saver:
    class: FileSerializer
    options:
        encoding: binary
        format: compact


connections:
  source.data = saver.data

...

string
bool
int
double
oneOf("binary", "yaml") // list of allowed strings
*/
// ---------- Option model (sealed, UI-friendly) ----------
// ---------- Option model (mutable for UI) ----------
// ---------- Option model (dynamic + type-safe) ----------
